bin/
dist/
*.db
*.db-wal
*.db-shm
.env
.DS_Store

# Client projects: WordPress / Laravel sites. Never commit.
projects/

# macOS Swift app build output (DevnerPrompt.app is a compile artifact).
macos/*/build/
